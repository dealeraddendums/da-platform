# deps
/node_modules

# next.js
/.next/
/out/

# env
.env
.env.local
.env.*.local

# misc
.DS_Store
*.pem
.vercel
tsconfig.tsbuildinfo

# Claude Code session state — not part of the repo
.claude/

# Box JWT app config (server-only, never in git)
box-config.json
box-private-key.pem
