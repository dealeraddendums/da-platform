# Rundown — 2026-06-09 (next-session opener)

## ✅ Shipped to prod this session
- **Billing price integrity** — da-billing is the sole price authority (closed the "honor
  caller-supplied price" hole, platform stopped sending prices, render guard); **re-canonicalized 27
  group/reseller templates** (Dealer General $7,937 → correct $16,125); customer-detail invoice-history
  bug fixed. *(da-billing `1ab622e`, da-platform `e5f2003`)*
- **DA Business Intelligence tab** (Phases 1+2) — metrics + PDF/Excel export + on-demand email;
  `converted_at` (migration 095) + da-billing gross-billable endpoint. *(`72b8419`, `b8c6d53`, da-billing `c03ddbd`)*
- **Fuel Type product rule** — curated multiselect + keyword matching (migration 097). *(`40eb01f`)*
- **Account-purpose classifier** (Real/Test/Sales Demo) — flags test/demo at creation (migration 096). *(`1357c63`)*
- **Group-billed dealer Billing view** — plan + payer, no empty state. *(`1780cee`)*
- **Invoice view/download** on Billing tabs (real PDF via da-pdf-service). *(`7d82cbe`)*
- **`?type` bulk-button fix** (infosheet/buyer_guide). *(`7d82cbe`)*
- **Impersonation security fix** — group-name click no longer auto-impersonates. *(`bc81d48`)*
- **Past-due print lock** + payer tooltips + cache fix *(`12765cd`/`990bfa8`/`0bb275b`)*; **group_admin
  Dashboard/Products scoping** confirmed *(`e06ef49`)*; **Freeze-ETL toggle** da-platform side, migration 094 *(`aadf1da`)*.
- **🟢 Zero-downtime deploy — da-platform DONE & PROVEN** *(`b1db255`)*: release dirs + `current` symlink +
  pm2 cluster reload + keep-5 + `rollback.sh`. All 3 verifies passed (live deploy **869/0**, rollback +
  **781/0** reload stress, broken-build aborts with `current` untouched). Health gate now polls real
  HTTP-200 + auto-reverts. Old process had **1303** crash-restarts today → new layout **0**.

## 🧹 Data ops
- 16 test/demo dealers flagged `is_test` (incl. 8 sales demos); 7 da-billing test customers archived
  ("Performance Test Account Do Not Delete" intentionally skipped).

## 🔍 Diagnosed & closed
- Addendum "sync gap" → dealers **genuinely dormant**, not a bug (#105 closed). Truth source = Aurora
  `dealer_inventory.PRINT_DATE`.

## 🟡 Open — next session
- **🔴 #115 ETL config-lock enforcement** (`da-legacy-etl`) — code committed (`5c028da`) but **deploy
  blocked**: the ETL box has **uncommitted live prod work** that `runner.ts` imports. Needs a dedicated
  **ETL-box reconciliation session** (get the box's work into git → rebase `5c028da` → deploy → flip
  Dealer General `etl_locked=true` → re-delete dupes). **Stopgap holding:** Mercedes Benz of Collierville
  = `migration_status='migrated'` (ETL skips it) + dupes deleted. The freeze toggle is **inert** until
  this ships. (`etl-config-lock.md` STATUS banner.)
- **✅ da-billing zero-downtime cutover — DONE 2026-06-10** (`f5b102f`, cluster:2; 3 verifies passed —
  live **530/0**, rollback in seconds, broken-build aborts + failed-dir cleaned). Health path = `/`;
  standalone+cluster resolved cleanly (no `instances:1` fallback needed). **Both da-platform AND
  da-billing are now on zero-downtime deploys + seconds-rollback — patchable mid-day.** Operator flow
  per box: `bash deploy.sh [commit]` to ship, `bash rollback.sh` to revert.
- **🟡 #116 session-layer impersonation rework** — built, **held for review** (5 files; group-level
  reset via a `da_group_level` cookie gate, no profile mutation). The security hole is already closed
  live; this is the "land at group level, not in a member dealer" polish. Deploy on the new zero-downtime path.

## 🔵 Decisions / manual (Allan)
- Schedule the **ETL-box reconciliation** (unblocks #115).
- **da-billing** cutover timing.
- **Feed-mapping pollution** — `dealer_vehicles.fuel` ~95% garbage + `printed_at` NULL on imported
  addendum_data: same class of feed-mapping bug; worth one combined pass **with Alex** (also corrupts
  displayed fuel on addendums).
- Optional later: **two boxes behind the ALB** (survive a bad deploy / box death); **per-action audit
  tag** for impersonated actions.

## 🔍 Verify — QA schedule
`qa-verification-2026-06-09.md` — **§0 (deploy mechanics) PASSED for da-platform.** Run §1 Billing +
§2 Print-lock + §3 BI (you/Alex), hand §4–§10 UI checks to Marlena on a **Test dealer**; §11 (ETL) +
the ⏳ part of §8 once #115 / the #116 rework land. Highest-value: the print-lock **false-positive**
check (a current dealer still prints) + billing reconcile (Dealer General = $16,125).

_Migration high-water: **097**._
