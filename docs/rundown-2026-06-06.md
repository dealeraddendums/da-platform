# Rundown — 2026-06-06 (next-session opener)

## ✅ Shipped to prod today
- **group_admin authorization hardening** (`b25bcbc`) — group-scoped reads on options/library,
  corporate-products, custom-sizes, addendum-library; active-dealer `billing/me*`; closed
  real cross-group leaks: print/[vehicleId], pdf/generate, dealers/[id]/logo, orders/labels
  (incl. a `dealer_admin` leak on labels-POST).
- **group_admin ↔ dealer full parity** (`6284de4` + UI follow-up + `dcacae2`) — new
  `lib/dealer-authz.ts` (resolveEffectiveDealer / authorizeDealerAction) routed through 13
  routes; dealer-profile `canEdit` (+ a cross-group read fix); Dealer Users provisioning
  (invite/edit/resend/revoke; delete/impersonate/mint-dealer_admin stay super_admin);
  disclaimers gap fixed; docs committed (`07fd4ef`). Principle now in root `CLAUDE.md`
  (group_admin-in-dealer = full dealer_admin; service-provider groups like Dealer General
  supported).
- **Platform ↔ da-billing link sync** — match key = `platform.billing_id` → da-billing
  customer id (NOT ClientID). Backfilled **1,760** links (157 groups + 1,603 dealers),
  synced 7→1,767, **zero duplicates**. Create-or-link **guard** deployed (link if billing_id
  resolves · soft email/name → 409 review block · create only truly-new). Dealer General now
  shows its real customer.
- **Dealer Help — Phase A** (migrations **091** help_articles + **092** help_conversations;
  `f135c00` + fixes) — dedicated help CMS (text + **video** embed/upload, TipTap authoring,
  strict iframe allowlist) at `/help`; **context-aware grounded assistant** (own-data-only,
  Haiku, guardrailed); **global Help/Support widget** on every page; conversation logging +
  👍/👎 + super_admin **review-and-correct-into-KB**; **async "talk to a person"** (Mandrill →
  support@); **full-transcript HubSpot logging** (one note/conversation, upsert). `/help`
  styling fix also shipped.
- **Legal pages** — entity **DealerAddendums LLC**, governing law **Delaware**, **support@**
  only, branded shell + **Download PDF**, live on app + marketing.

## 🟡 Specced, not yet built (queued)
- **Help/Support Phase B** (`help-support-widget.md`) — real-time live chat (Supabase
  Realtime) + AI→human takeover + **support-agent client for desktop + iPhone** (responsive
  PWA, web push, SMS/email backstop, On→notify availability). Biggest remaining lift;
  build-vs-buy on the chat transport is an open call.
- **StackMonitor status** (`stackmonitor-integration.md`) — read API **built** in the
  StackMonitor repo; still to do: stand up the monitors + the `?tag=public` migration, and
  the **DA consumer + `/api/health/deep`** (prompt given, not built) → public
  `status.dealeraddendums.com` + in-app status dot.
- **builder-inventory-followups-0606** — CMPG/HMPG fields + VIN-decode populate; persistent
  inventory filters.
- **Confirm** the Dashboard/Products active-dealer scoping (#71) actually deployed.

## 🔵 Open decisions / manual (Allan)
- **Billing-link worklist:** 4 Greenway collision dealers (which platform row is canonical —
  likely duplicate rows) + the self-billed 1:1 soft-match candidates (Payne, Plattner,
  Metrotech, Gettel Ocala, Prescott Honda, Wyatt Johnson) to confirm-link.
- **billing (i) link-candidate UI + open billing to group_admin (#4)** — staged/uncommitted;
  the guard it depends on is live, so it's safe to ship.
- **Marketing DNS cutover** for the legal/marketing pages; **legal-counsel review** before
  treating the legal docs as binding.

## 🔍 Verify — mostly done
- **Help Phase A verification (`096aaa7`):** **unit 17/17 + chat 2/2 PASS on prod** (run
  27073781946) — sanitizer/XSS allowlist, own-data/ownership scoping, and off-topic decline all
  green. **Pending (need QA values, ~5 min tomorrow):** the HubSpot-upsert check (needs
  `QA_USER_ID` + `QA_TEST_HUBSPOT_CONTACT_ID` — a QA profile pointing at a TEST HubSpot contact)
  and a rerun against a **Trial/Free** QA dealer to exercise the "printing PAUSED — upgrade"
  path (qa-test-dealer-a is Paid, so it only hit the enabled branch). Plus a 30-sec visual
  glance: widget opens, video renders on `/help`.

_Migration high-water: **092**._
